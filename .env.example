# -------------------------------------------------
# PostgreSQL
# -------------------------------------------------
DB_NAME=myproject
DB_USER=myprojectuser
DB_PASSWORD=mysecretpassword
DB_HOST=localhost
DB_PORT=5432

# -------------------------------------------------
# Django (backend)
# -------------------------------------------------
DJANGO_SECRET_KEY=django-insecure-CHANGE_ME
DJANGO_DEBUG=True               # False в продакшн
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Если используете gunicorn, можно задать количество воркеров
GUNICORN_WORKERS=3

# -------------------------------------------------
# Svelte/Vite (frontend) – при необходимости
# -------------------------------------------------
VITE_API_URL=http://localhost:8000/api   # URL, к которому фронтенд будет обращаться
